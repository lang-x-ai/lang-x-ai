// Command-line interactive calculator taking two inputs to perform calculations
const calculator = (x, y) => {
    const add = (x, y) => x + y;
    const sub = (x, y) => x - y;
    const mul = (x, y) => x * y;
    const div = (x, y) => x / y;
};
