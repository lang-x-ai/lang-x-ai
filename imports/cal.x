lang = "cpp";
Calculator = func(x,y){
  prompt "Need a scientific calculator that";
  Add = func(x, y) {x+y};
  Add = func(x, y) { x - y};
  Add = func(x, y) { x + y };
  Add = func(x, y) { x / y};
};

