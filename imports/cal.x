calculator = λ(x, y) {
  prompt "have a commandline interactive calculator, which takes two inputs and does some calcuations.";


     add = λ(x, y) { x + y };
     sub =  λ(x, y) { x - y };
     mul = λ(x, y) { x * y };
     div = λ(x, y) { x / y};
};

